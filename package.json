{
  "name": "daily-token-giveaway-frame",
  "version": "0.1.0",
  "description": "Daily token giveaway frame for Warpcast",
  "main": "src/App.tsx",
  "scripts": {
    "start": "frames serve",
    "build": "frames build"
  },
  "dependencies": {
    "frames.js": "^latest",
    "warpcast": "^latest",
    "@solana/web3.js": "^latest"
  },
  "devDependencies": {
    "ts-node": "^latest", // Only needed if using TypeScript
    "typescript": "^latest" // Only needed if using TypeScript
  }
}
